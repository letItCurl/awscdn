<p id="notice"><%= notice %></p>

<p>
  <strong>Title:</strong>
  <%= @post.title %> <br>
  <%= @post.picture.attached? %> <br>
  <img src="<%= url_for(@post.picture) if @post.picture.attached? %>" style="width: 300px;" alt="Italian Trulli">
</p>

<%= link_to 'Edit', edit_post_path(@post) %> |
<%= link_to 'Back', posts_path %>
